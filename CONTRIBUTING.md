# Contributing

Issues and focused pull requests are welcome. Before submitting a change, run:

```powershell
npm install
npm run test
npm run build
```

Keep machine-readable output stable, preserve documented exit codes, and never add commands that silently overwrite or retry canvas mutations.
